// String
const ejemplo = 'Esto es un texto'

const minuscula = ejemplo.toLowerCase() 

const arr = ejemplo.split(' ')

const reemplazar = ejemplo.replace('texto', 'ejemplo')


// Arrays

const incluir = arr.includes('ejemplo')

const duplicado = arr.slice(3)

arr.splice(0,2)

arr.push('de')

const last = arr.pop()

arr.unshift('Esto')

Array.from()

Array.isArray(arr)