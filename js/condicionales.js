if (condicion) {
    // se ejecuta lo que hay dentro
} else {
    // se ejecuta si la condición NO se cumple
}


if (condicion) {
    if (condicion2){

    }
}


if (condicion1) {
    // Si se cumple la 1
} else if (condicion2) {
    // Si se cumple la 2
} else {
    // Si no se cumplen ni la 1 ni la 2
}

if (x > y && y > z) {

} else if (x > y || y > z) {

}

condicion ? 'si se cumple condicion me ejecuto' : 'si no se cumple condición me ejecuto'


//falsy values -> No valen nunca como condiciones
false, 0, -0, '', null, undefined, NaN

//coertion
!!0 // false
!!null // false

! // Not

!0 // True